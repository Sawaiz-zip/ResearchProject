Implement a module named half_adder with the following interface. All input and output ports are one bit.

 - input  a
 - input  b
 - output sum
 - output cout

The module should implement a half adder. A half adder adds two bits (with no carry-in) and produces a sum and carry-out.
sum  = a XOR b
cout = a AND b
